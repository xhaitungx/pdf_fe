export interface MenuPopupProps {
  readMode: number;
}

export interface MenuPopupStates {
  isOpenMenu: boolean;
  text: string;
  noteRect: any;
  pageWidth: string;
  pageHeight: string;
  pageX: number;
  pageY: number;
}
